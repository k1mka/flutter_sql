import 'package:flutter/material.dart';
import 'package:flutter_sql/presentation/widgets/tokens/tokens.dart';

class Radiuses {
  static const borderRadius4 = BorderRadius.all(Radius.circular(Tokens.xxS));

  static const borderRadius8 = BorderRadius.all(Radius.circular(Tokens.S));

  static const borderRadius12 = BorderRadius.all(Radius.circular(Tokens.M));

  static const borderRadius16 = BorderRadius.all(Radius.circular(Tokens.L));

  static const borderRadius32 = BorderRadius.all(Radius.circular(Tokens.xxxL));
}
